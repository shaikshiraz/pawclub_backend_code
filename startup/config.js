const config=require('config');

module.exports=function(){
    // if(config.get('jwtPrivateKey')){
//       console.error("Fatal Eror: private key is not defined");
//         process.exit(1);
//       }
}